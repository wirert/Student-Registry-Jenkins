pipeline {
    agent any
    stages {
        stage('Test pipe') {
            steps {
                echo 'Hello from the Pipe!'
            }
        }
    }
    // post {
    //     always {

    //     }
    // }
}